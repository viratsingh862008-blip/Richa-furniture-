function clean(v:any){return String(v??"").trim()}export default async function handler(req:any,res:any){if(req.method!=="POST")return res.status(405).json({ok:false});const b=req.body||{};const name=clean(b.name),phone=clean(b.phone),room=clean(b.room),message=clean(b.message);if(name.length<2||!/^[+0-9()\-\s]{8,18}$/.test(phone))return res.status(400).json({ok:false,message:"Enter a valid name and phone number."});const payload={id:crypto.randomUUID(),name,phone,room,message,createdAt:new Date().toISOString()};try{if(process.env.LEAD_WEBHOOK_URL)await fetch(process.env.LEAD_WEBHOOK_URL,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(payload)});return res.status(200).json({ok:true,id:payload.id})}catch{return res.status(500).json({ok:false,message:"Could not send your room brief."})}}