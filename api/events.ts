export default async function handler(req:any,res:any){if(req.method!=="POST")return res.status(405).json({ok:false});try{const body=req.body||{};if(process.env.ANALYTICS_WEBHOOK_URL)await fetch(process.env.ANALYTICS_WEBHOOK_URL,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({...body,receivedAt:new Date().toISOString()})});return res.status(200).json({ok:true})}catch{return res.status(400).json({ok:false})}}